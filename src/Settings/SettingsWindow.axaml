<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:AudioToggle"
        x:Class="AudioToggle.SettingsWindow"
        Title="Audio Toggle - Settings"
        Width="500"
        Height="700"
        Icon="avares://AudioToggle/assets/tray_icon.ico">
        <TabControl>
        <TabItem Header="Output Devices">
            <StackPanel>
                <TextBlock Text="Audio Devices:" Margin="10" />
                <TextBlock Text="Check devices to include in hotkey cycling:" Margin="10,0,10,5" FontSize="12" Foreground="Gray"/>
                <ScrollViewer Margin="10" Height="250" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <ItemsControl x:Name="AudioDevicesListBox">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type local:DeviceViewModel}">
                                <StackPanel Orientation="Horizontal" Margin="0,2">
                                    <CheckBox IsChecked="{Binding IsEnabled}" Margin="0,0,10,0" VerticalAlignment="Center"/>             
                                    <TextBlock Text="{Binding Name}" VerticalAlignment="Center" PointerPressed="DeviceText_PointerPressed"/>
                                    <TextBlock Text="  active  " Foreground="Green" Margin="0,0,5,0" IsVisible="{Binding IsDefault}" VerticalAlignment="Center"/>
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
                
                <Separator Margin="10"/>
                
                <TextBlock Text="Hotkey Settings:" Margin="10" FontWeight="Bold" />
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Text="Switch Output Device:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
                    <TextBox x:Name="OutputHotkeyTextBox" Grid.Row="0" Grid.Column="1" Text="Ctrl+Shift+F1" IsReadOnly="False" Margin="5" Watermark="Press keys to set hotkey..."/>
                    
                    <Button x:Name="ResetOutputHotkeyButton" Grid.Row="1" Grid.Column="1" Content="Reset to Default" Margin="5" HorizontalAlignment="Left"/>
                </Grid>
            </StackPanel>
        </TabItem>
        
        <TabItem Header="Input Devices">
            <StackPanel>
                <TextBlock Text="Input Devices:" Margin="10" />
                <TextBlock Text="Check devices to include in hotkey cycling:" Margin="10,0,10,5" FontSize="12" Foreground="Gray"/>
                <ScrollViewer Margin="10" Height="250" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <ItemsControl x:Name="InputDevicesListBox">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type local:DeviceViewModel}">
                                <StackPanel Orientation="Horizontal" Margin="0,2">
                                    <CheckBox IsChecked="{Binding IsEnabled}" Margin="0,0,10,0" VerticalAlignment="Center"/>             
                                    <TextBlock Text="{Binding Name}" VerticalAlignment="Center" PointerPressed="InputDeviceText_PointerPressed"/>
                                    <TextBlock Text="  active  " Foreground="Green" Margin="0,0,5,0" IsVisible="{Binding IsDefault}" VerticalAlignment="Center"/>
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
                
                <Separator Margin="10"/>
                
                <TextBlock Text="Hotkey Settings:" Margin="10" FontWeight="Bold" />
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Text="Switch Input Device:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
                    <TextBox x:Name="InputHotkeyTextBox" Grid.Row="0" Grid.Column="1" Text="Ctrl+Shift+F2" IsReadOnly="False" Margin="5" Watermark="Press keys to set hotkey..."/>
                    
                    <Button x:Name="ResetInputHotkeyButton" Grid.Row="1" Grid.Column="1" Content="Reset to Default" Margin="5" HorizontalAlignment="Left"/>
                </Grid>
            </StackPanel>
        </TabItem>
        
        <TabItem Header="General">
            <StackPanel>
                <TextBlock Text="General Settings:" Margin="10" FontWeight="Bold" />
                <CheckBox x:Name="StartWithWindowsCheckBox" Content="Start with Windows" Margin="10"/>
                <CheckBox x:Name="ShowNotificationsCheckBox" Content="Show device change notifications" Margin="10" IsChecked="True"/>
                
                <Separator Margin="10"/>
                
                <TextBlock Text="Updates:" Margin="10" FontWeight="Bold" />
                <CheckBox x:Name="AutoUpdateCheckBox" Content="Automatically check for updates" Margin="10" IsChecked="True"/>
                <Button x:Name="CheckForUpdatesButton" Content="Check for Updates Now" Margin="10" HorizontalAlignment="Left"/>
                
                <Separator Margin="10"/>
                
                <TextBlock Text="About:" Margin="10" FontWeight="Bold" />
                <TextBlock Text="AudioToggle v1.1.0" Margin="15,5"/>
                <TextBlock Text="Quick audio toggle utility by Jeremy Costen" Margin="15,5" Foreground="Gray"/>
                <Button x:Name="GitHubLinkButton" Content="https://github.com/jcosten/audiotoggle/" Margin="15,5" Background="Transparent" BorderThickness="0" Foreground="LightBlue" Cursor="Hand" Click="GitHubLinkButton_Click"/>
            </StackPanel>
        </TabItem>
    </TabControl>
</Window>
